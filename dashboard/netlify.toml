[build]
  base = "dashboard"
  command = "npm run build"
  publish = "dist"

# SPA fallback — all routes serve index.html (client-side routing).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
